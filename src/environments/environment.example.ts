/**
 * This file serves as an example environment configuration.
 *
 * Rename this file to `environment.ts` and replace placeholder values with your actual API keys or environment-specific variables.
 * Make sure `environment.ts` is excluded from version control by adding it to your `.gitignore`.
 *
 * This file is intended to store environment-specific values such as third-party service API keys.
 */
export const environment = {
  googleMapsApiKey: 'REPLACE_WITH_YOUR_API_KEY'
};
